<Page
    x:Class="DubHero_UI.Vistas.Juego"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DubHero_UI.Vistas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
        RequestedTheme="Light"
>
    <Page.Resources>
        <Style TargetType="Polyline" x:Key="propLineas">
            <Setter Property="Stroke" Value="Black"/>
            <Setter Property="StrokeThickness" Value="7"/>
        </Style>
    </Page.Resources>
    <RelativePanel>
        <Canvas x:Name="pistaObjetivo">
            <Polyline x:Name="l1" Style="{StaticResource propLineas }" Points="600,0,150,1000"/>
            <Polyline x:Name="l2" Style="{StaticResource propLineas }" Points="650,0,400,1000"/>
            <Polyline x:Name="l3" Style="{StaticResource propLineas }" Points="700,0,650,1000"/>
            <Polyline x:Name="l4" Style="{StaticResource propLineas }" Points="750,0,900,1000"/>
            <Polyline x:Name="l5" Style="{StaticResource propLineas }" Points="800,0,1150,1000"/>
            <Polyline x:Name="l6" Style="{StaticResource propLineas }" Points="850,0,1350,1000"/>
        </Canvas>
        <Line X1="0" X2="1200" RelativePanel.AlignBottomWithPanel="True" Margin="150,20" StrokeThickness="4" Fill="Black"/>
        <Ellipse Width="190" Height="190" Fill="Aqua" RelativePanel.AlignBottomWithPanel="True" Margin="230,30"/>
    </RelativePanel>

    <!--<Canvas x:Name="miCanvas" >
        <StackPanel Orientation="Horizontal">
            <Grid  x:Name="pista1" BorderBrush="Red" BorderThickness="1" Width="125" Height="900"/>
            <Grid x:Name="pista2" BorderBrush="Red" BorderThickness="1" Width="125" Height="900"/>
            <Grid x:Name="pista3" BorderBrush="Red" BorderThickness="1" Width="125" Height="900"/>
            <Grid x:Name="pista4" BorderBrush="Red" BorderThickness="1" Width="125" Height="900"/>
            <Grid x:Name="pista5" BorderBrush="Red" BorderThickness="1" Width="125" Height="900"/>
        </StackPanel>
    </Canvas>-->

</Page>
